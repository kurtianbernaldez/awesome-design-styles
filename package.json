{
  "name": "design-style-mds",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Original, complete visual design systems with comparable variants",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/build.mjs && node --check dist/app.js && node --check dist/preview.js && node --check dist/pointer-effects.js && node --check dist/pointer-kinetics.js && node --check dist/liquid-glass.js && node --check dist/liquid-adaptation.js && node --check dist/liquid-transitions.js && node --test tests/*.test.mjs"
  }
}
